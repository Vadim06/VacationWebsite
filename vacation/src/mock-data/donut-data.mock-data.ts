import { APP_COLORS } from '../constants/constants';

const sickDaysLeftMockData = {
  daysLeft: 10,
  daysAll: 30,
  donutColor: APP_COLORS.red,
};

const vacationDaysLeftMockData = {
  daysLeft: 5,
  daysAll: 10,
  donutColor: APP_COLORS.blue,
};

export { sickDaysLeftMockData, vacationDaysLeftMockData };
