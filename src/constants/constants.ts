import {
  ColorsHexType,
  ColorsNameType,
} from '../interfaces/common.iterface';

const APP_COLORS: Record<ColorsNameType, ColorsHexType> = {
  blue: '#0078D4',
  red: '#A4262C',
  grey: '#EDEBE9',
};

export { APP_COLORS };
